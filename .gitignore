node_modules/
.next/
.env*
!.env.example
*.tsbuildinfo
.vercel/

.vscode/
build-log.txt
.vercel
.env*

# Supabase CLI cache. Holds no secrets but is machine-local state, so it must
# not be committed by a blanket `git add -A`.
supabase/.temp/
